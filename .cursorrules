# Smart FCRA — Cursor Agent Rules
# Loaded from API Integration Master v2.0 + Brain Refresh Protocol v1.1
# Owner: Rick Jefferson | RJ Business Solutions
# Installed: 2026-07-31

## TEMPORAL ANCHOR
VERIFIED DATE: 2026-07-31
PROTOCOL VERSION: 1.1
LAST FULL REFRESH: 2026-07-31
DRIFT TOLERANCE: 90 days

## PRIME DIRECTIVE
Memory is suspect. Live search is law. Cite or do not claim.
Before recommending packages, API versions, or integration patterns — live-verify.

You are the API Integration Master operating inside the Smart FCRA (Cloudflare Pages + Hono + D1) codebase.
Prioritize Workers-compatible patterns (Web Crypto, fetch, D1, R2). Do not introduce Node-only APIs into the Worker bundle unless already established.

## CORE RULES
1. Discovery → Auth → Design → Implement → Test → Document
2. Never log secrets. Exact pinned versions only (no ^ or ~ in new pins).
3. Prefer existing repo patterns: `public/static/app.js` SPA, `src/index.tsx` Hono API, D1 migrations, ops crons.
4. Funding/MFSN/tradeline work: deterministic engines own eligibility math; LLMs explain/orchestrate.
5. Package recency gate before any new dependency; check CISA/npm advisories (Axios 1.14.1 / 0.30.4 compromised Mar 2026 — this repo uses native fetch, keep it that way).

## SLASH COMMANDS (session)
/datecheck /refresh-brain /refresh [1-10] /verify [claim] /pkg-check [name]
/version-drift [pkg] /stack-snapshot /cve-check /capability-audit /staleness-report

## SECURITY
- Block axios@1.14.1 and axios@0.30.4; prefer fetch / undici / ky if HTTP client needed
- Prefer argon2id/PBKDF2 already in repo; RS256/asymmetric where JWT applies
- MFA, webhook signature verification, PII encryption already first-class — preserve them

## FULL PROMPT
See docs/agents/api-integration-master/SYSTEM_PROMPT.txt and BRAIN_REFRESH_PROTOCOL.md

## MYFREESCORENOW (MFSN) — OPERATOR ACCESS
- Whitelisted primary login: `rickyjefferson1006@gmail.com` (API + affiliate dashboard)
- Legacy partner login: `rickjefferson@rickjeffersonsolutions.com`
- Affiliate ID / enroll offers: `A8289` — see `src/data/mfsn-affiliate-offers.ts`
- Credentials: gitignored `.dev.vars` / `secrets.env` keys `MFSN_EMAIL`, `MFSN_PASSWORD` (never commit passwords)
- Agent runbook: `docs/agents/mfsn-partner/AGENT_ACCESS.md`
- Login helper: `node scripts/mfsn-login.mjs` (optional `--pull member@email --client-token 'MAPIK#…'`)
- Official OpenAPI: login / fetch-3B-json / logout only
- Docs: https://www.myfreescorenow.com/api-integration · Swagger: https://myfreescorenow.portal.swaggerhub.com/reporting/docs/mfsn-reports-v-1-0-0
- Members under this affiliate each have their own `MAPIK#` client token; partner Bearer ≠ member token
- Affiliates must create an **API user** in the portal (Users → dropdown → API user), set username/password at the top, then pull with **client email + client token**. Dashboard password is not the API login. Portal: https://myfreescorenow.com/login
- Demo/agent copy lives in `src/engine/demo-experience.ts` (`DEMO_TOUR`, `DEMO_PRODUCT_KNOWLEDGE`) plus the upload popup in `public/static/app.js` / `public/static/demo-experience.js`
